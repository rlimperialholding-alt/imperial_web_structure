# Managed by Imperial AI ADAS v2.3.3. Do not edit this generated file directly.
name: Imperial ADAS
on:
  pull_request:
  merge_group:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  deterministic-gates:
    name: Deterministic gates
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v7.0.1
        with:
          fetch-depth: 0
      - name: Run Phase 1 and Phase 2 deterministic checks
        shell: pwsh
        run: ./.imperial-adas/ci/Invoke-ADASCI.ps1
      - name: Upload CI attestation
        if: always()
        uses: actions/upload-artifact@v7.0.1
        with:
          name: imperial-adas-ci-attestation
          path: ci-attestation.json
          if-no-files-found: error